{
  "name": "velocity-donasi",
  "version": "3.0.0",
  "description": "Build and tooling for Velocity Donasi front-end assets.",
  "private": true,
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "echo \"No lint configured\" && exit 0",
    "build": "npm-run-all build:clean build:copy",
    "build:clean": "rimraf dist",
    "build:copy": "cpx \"js/**/*.js\" dist/js && cpx \"css/**/*.css\" dist/css"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5"
  },
  "dependencies": {}
}
